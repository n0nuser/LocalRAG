[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "localrag-example-retriever"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["localrag"]

[project.entry-points."localrag.retrievers"]
example = "localrag_example_retriever:plugin"
